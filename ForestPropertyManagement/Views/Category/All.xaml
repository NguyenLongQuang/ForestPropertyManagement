<UserControl x:Class="ForestPropertyManagement.Views.Category.All"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ForestPropertyManagement.Views.Category"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel>
            <Label x:Name="title">Danh muc
            </Label>

            <StackPanel Orientation="Horizontal">
                <Button Click="FacilityStats_Click">Thống kê cơ sở</Button>
                <Button Click="FacilityMap_Click">Xem bản đồ cơ sở</Button>
                <Button Click="FacilityReportMonth_Click">Thống kê cơ sở theo tháng</Button>
            </StackPanel>
            <ListView x:Name="ListView" ItemsSource="{Binding Path=List}"
                      SelectionMode="Single"
                      SelectionChanged="Selected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding Path=FormerName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
